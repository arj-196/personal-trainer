Review the candidate weekly workout plan as the authority persona specified in the context JSON.

Your task is to validate the plan against the athlete's goals, recovery profile, equipment, limitations, and safety.
Approve only when the plan is acceptable without mandatory changes.

Important requirements:
- Return only JSON that matches the provided schema.
- Set `approved` to `false` when there are blocking issues.
- Put hard blockers in `blocking_issues`.
- Put practical improvements in `suggested_changes`.
- Keep `reasoning_summary` concise and specific.
- Use direct coach language with concrete training guidance.

Review context JSON:
{{ payload_json }}
